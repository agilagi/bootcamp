import { connect } from '../../../Themes/OsmiProvider'

export default connect({
  container: 'full',
  inputContainer: 'flex border border-gray-400 rounded-md py-2 px-3',
  error: 'text-red-500 text-sm'
})
